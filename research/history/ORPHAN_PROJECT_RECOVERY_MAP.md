# Orphan Project Recovery Map

Status: `HISTORICAL_RECOVERY / NON-AUTHORITY`

This map preserves older Library code identities that were omitted or compressed by later project taxonomies. It is an index of source-native recovery evidence, not a statement that any two entries are the same project.

## Governing rules

- `UNKNOWN != ABSENT`.
- A later family label does not erase an earlier source-native project identity.
- Semantic similarity is not identity.
- A recovered predecessor remains historically itself even if later code reuses or reframes its mechanisms.
- `identity_with_fuzzball` remains `UNRESOLVED` unless an exact historical carrier or provenance edge establishes it.

## ORPHAN-001 — FractalDeflectiveEngine / bumper-car predecessor

**Library source carrier**

- name: `Pasted text(9).txt`
- Library file id: `file_0000000088a8720ca7a90763e9613aa1`
- Library created_at: `2026-07-15T12:39:27Z`
- source namespace: `FractalDeflectiveEngine`

**Recovered implementation identity**

The standalone source contains `FractalManifoldTrack3D`, `DataLaneField`, `FlowState`, `FlowParameters`, `DataFlowTrackedDeflectiveEngine3D`, and `CurriedFractalEngine`. It models a 3D fractal/polyline track, data-driven steering, interacting flow states, collision/correction, jolt accumulation, mass/inertia, quadrant-energy output, and kinetic plasticity.

**Independent downstream provenance**

The later `adaptive-connectivity-diagnostic-network.zip` contains:

- `docs/MIGRATION_FROM_BUMPER_CAR.md`
- `legacy/OriginalBumperCarEngine.cs`

That migration explicitly treats the original user-provided “bumper-car” engine as a predecessor and maps its state/interaction machinery into the later adaptive-connectivity network.

**Recovery consequence**

Later historical machinery indexes represented the downstream ACDN family but did not preserve `FractalDeflectiveEngine` as a named source-native project. This proves that later family catalogs can omit genuine earlier project identities.

**Fuzzball relation**

`identity_with_fuzzball: UNRESOLVED`

No direct `Fuzzball`, `fuzz ball`, or equivalent project marker has yet been recovered from this source or its migration record. Do not relabel it by resemblance.

---

## ORPHAN-002 — GrandUnifiedPerceptron / Adaptive Simulation predecessor

**Library source carrier**

- name: `grand_unified_perceptron.py`
- Library file id: `file_000000009390720cb80ed1f6bb0d7a4f`
- Library created_at: `2026-07-15T20:08:53Z`

**Recovered implementation identity**

The source identifies itself as a single-cell `Grand Unified Perceptron` simulation and combines:

- continuous Hopfield relaxation;
- Oja/Hebbian feature learning;
- inertial parameter dynamics;
- Rule-110 topology mutation;
- structural `Seek` / `Prune` actions;
- separated fast/slow timescales;
- deterministic replay and explicit traces.

**Downstream branch**

`Pasted text(10).txt` and `Pasted text(11).txt` on 2026-07-17 review and narrow the design into a more conservative `Adaptive Simulation Numerical Core` package. The chronology therefore establishes a predecessor/reframing relation rather than one timeless project label.

**Fuzzball relation**

`identity_with_fuzzball: UNRESOLVED`

No direct Fuzzball project marker has been recovered in these carriers.

---

## GAP-001 — Original structured solver/operator lab source

A separate Operator Moonshot release record, `RELEASE.json` (`file_000000009f7081f7b147f766a120c8c0`), records:

`original_structured_solver_operator_lab_source: known_gap`

This is retained as its own historical gap. It must not be collapsed into Fuzzball without evidence.

`identity_with_fuzzball: UNRESOLVED`

---

## Recovery-system finding

The 2026-09-06 Library consolidation explicitly defined `UNASSIGNED FAMILY` as material that must remain in place until its own authority/lineage can be recovered. Its later 12-record cold root-edge sample found five records still `NOT_YET_ASSIGNED`.

Therefore later project taxonomies and recovery folders are navigation aids, not exhaustive inventories of historical project identity.

## Next recovery boundary

Continue source-native recovery through:

1. loose-root generic source snapshots;
2. internal `legacy/`, `migration`, `original`, and predecessor paths in later archives;
3. unassigned root-edge carriers;
4. compact historical/source/recovery packages that may preserve old names;
5. exact Library chronology and file IDs.

Promote a Fuzzball identity only when an exact source, archive member, historical pointer, or independently supported provenance relation names or unmistakably identifies that project.
