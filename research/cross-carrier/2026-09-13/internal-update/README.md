# Internal search and cross-carrier update

[Open Orbit Search](global_search/index.html) · [Run locally](global_search/README.md) · [Complete recovery bundle](internal_connection_test_2026-09-13.zip) · [Exact material inventory](MATERIALS.json)

This successor adds the completed internal-workspace materials without replacing the current Space Lens, voice, Compass4D, coordinate workbench, or research corpus. The directly committed app sources are reviewable here. The original ZIP is an exact recovery carrier, not a substitute for those sources or a claim of promotion.

| Material | Representation | Evidence boundary |
| --- | --- | --- |
| Search catalog and simplified interface | `global_search/` source | 65 enabled services, 248 country/territory routes; Russia excluded; not 248 independently owned engines |
| Live retrieval adapters | `search.py`, `live_results.json` | Crossref/Europe PMC metadata; retrieval is not independent claim verification |
| Separate headless workspace | `shadow.cjs`, `serve.py`, receipts and tests | Real XY mouse/touch events on internal fixture; no access to current tabs or signed-in sessions |
| Public-page inspection | Explicit worker read mode | Proxy certificate validation failed in original environment; unrestricted internet is not established |
| Black-hole view | `space.js` and preserved legacy project | Geometric 4D-to-3D projection, not physical relativity or a Hodge result |
| Working-packet import | `import_working_packet.py`, sample and receipt | Exact TBCL transport into Orbit quarantine; no authority transfer |
| Connected transport continuation | `transport-README.md`, `transport-coverage.json`, full sources in recovery bundle | 39 exact reconstructions and 39 tampering rejections reported from the preceding executed run |
| Strict corroboration candidate | `connection_next/framework/` in recovery bundle | Unpromoted: 36 passes and 11 legacy failures retained |
| Earlier baseline and historical sources | Complete recovery bundle plus every-member inventory | Preserved source identity; not all branches were executed |

## Reconstruct the complete local workspace

Extract the recovery ZIP into a new directory, preserving its paths. This restores `connection_next/` alongside `global_search/`, as required by the working-packet importer and transport scripts. The directly committed `global_search/` is also sufficient for search and browser-worker operation. GitHub Pages provides the static links and scene; Python/Node services run on the user's machine and are not hosted by Pages.

`MATERIALS.json` accounts for every original member, including historical archives, generated receipts, database state and temporary transport carriers. Their inclusion is provenance, not an instruction to execute every historical file. `BUNDLE_MANIFEST.sha256` retains the original bundle's digest accounting. Original reports are not retroactively described as re-executed during publication.

Knowledge Decay remains active: provider availability, permissions, certificates, context and downstream usefulness must be rechecked when a task depends on them. A recovered record is not evidence of learning or human benefit.
