# Hodge frontier correction — September 14, 2026

Status: `SOURCE_DRIVEN_STATE_CORRECTION / OPEN_PROBLEM_STILL_UNRESOLVED`

## Why the state changes

The previous local Hodge decision field carried the Fermat-fourfold residual routes at `m=33`, `m=39`, and `m=45` as active conditional research obligations.

A newer primary preprint changes that state:

Rifat Jumagulov, **The Hodge conjecture for Fermat fourfolds of odd degree at most 199**, arXiv:2608.18134v1 (submitted July 28, 2026).

The paper proves, within its stated range:

- the zero-sum-triple / `*`-split closure used by the two `m=39` residual characters;
- the two-pair closure used by the residual `m=45` character;
- the level-lift closure used by the `m=33` character;
- and consequently the Hodge conjecture for every odd-degree Fermat fourfold with `3 <= m <= 199`.

Our local exact `m=39` check independently verifies the finite character hypothesis for both previously retained representatives over all `24` units modulo `39`. The algebraicity implication remains source-dependent on the external theorem rather than locally reproved.

Therefore the old odd residuals must no longer be presented as current unresolved targets. Preserve them as calibration/provenance states:

```text
m=33 -> EXTERNAL_THEOREM_CLOSED / local finite evidence preserved
m=39 -> EXTERNAL_THEOREM_CLOSED / local theorem-hypothesis check executed
m=45 -> EXTERNAL_THEOREM_CLOSED / prior finite route preserved
```

## New Fermat-fourfold frontier

The same author's companion manuscript and exact ancillary package,

**Residual gap classes in the even-degree Fermat-fourfold census through degree 250: exchange walls and two closures** (July 2026),

maps the even sector through degree `250`.

After its declared base predicates, bounded/deep closure family, exchange scan, and two additional closures, the remaining boundary is eight primitive certified gap classes organized into three claim-equivalence walls:

### W_70

- level 70: `(1,20,24,42,61,62)`
- level 210: `(2,9,129,142,168,180)` — exchange-equivalent to the 3-lift of the level-70 class

### W_110

- level 110: `(1,24,62,71,81,91)`
- level 110: `(1,31,55,71,81,91)`
- level 220: `(1,62,111,142,162,182)` — equivalent to lifted level-110 members

### W_114

- level 114: `(1,7,78,79,86,91)`
- level 114: `(1,13,43,72,103,110)`
- level 114: `(1,13,43,80,102,103)`

For every listed class the companion records

```text
nu(a) not in S_m
2 nu(a) in S_m.
```

The second relation is expected from the cited gap-group theorem; the unresolved content is algebraicity despite non-membership in the standard lattice.

## Decision-field reduction

The companion's Exchange Theorem proves claim-equivalence under certified exchanges. Therefore the eight residual class states may be quotiented, **for the algebraicity decision only**, to three wall states:

```text
{8 class records} / certified claim-equivalence
    -> {W_70, W_110, W_114}.
```

This is not a semantic similarity merge. It is a theorem-backed quotient of the target decision.

## Active target

The active Fermat research obligation is now:

> close one representative from each of `W_70`, `W_110`, and `W_114`, or produce a rigorous obstruction to the attempted closure mechanism; a closure of one member propagates only through the proved wall equivalences.

The strongest next route suggested by the companion's successful `m=168` closure is a **coset-transfer search**:

```text
find beta with known algebraicity claim
such that
nu(a) - nu(beta) in S_m,
```

while `nu(a)` and possibly `nu(beta)` may each lie outside `S_m`.

The `m=168` case proves this mechanism can close a genuine gap class. It does not establish that such a `beta` exists for any of the three open walls.

## Alternative routes kept live

1. **Coset transfer beyond exchange:** search for a known algebraic block in the same `S_m` coset.
2. **New exchanger family:** extend only with an independently algebraic exchanger and prove claim-equivalence before scanning.
3. **Lift/descent outside the existing bounded campaign:** only with explicit compatibility and a genuinely new level/mechanism.
4. **Gap-group geometry:** understand the order-two quotient class geometrically instead of treating `2 nu in S_m` as an algebraicity proof.
5. **Counterexample route to a proposed mechanism:** preserve negative searches as mechanism obstructions, not Hodge counterexamples.

## Evidence firewall

No P-versus-NP theorem, SAT computation, affine-branching result, or complexity evidence participates in the mathematical claims above. Shared decision-field language is `METHOD_ONLY`.

## Claim ceiling

- Odd Fermat fourfolds through degree 199: externally proved by the cited 2026 preprint; local work has not independently reconstructed the whole proof.
- Even Fermat boundary through degree 250: source-reported computer-assisted census and proved exchange/coset statements, with exact ancillary package; not independently regenerated in full here.
- `W_70`, `W_110`, `W_114`: active unresolved Fermat-fourfold targets under the checked source state.
- General Hodge conjecture: unresolved.